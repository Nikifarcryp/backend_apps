How to launch the project:
1. write "source fastapi_first_project/.venv/bin/activate"
2. pip install -r requirements.txt
3. open 2 terminal windows
type "uvicorn main:app --reload" to run backend application in the 1st terminal
type "npm run serve" to run frontend application in the 2nd terminal
4. move on to your local url and check the app!!!